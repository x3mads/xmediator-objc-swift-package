// Intentionally empty.
// This wrapper target exists so the XMediatorObjC product can combine the
// XMediatorObjC binary target and carry their required
// system-framework link settings (binary targets cannot declare either).
